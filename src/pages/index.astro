---
import Layout from "../layouts/Layout.astro";
import PostCard from "../components/PostCard.astro";
import { getCollection } from "astro:content";

const posts = (await getCollection("posts")).sort(
	(a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf(),
);
---

<Layout title="Web3Blog - El Futuro Descentralizado">
	<section class="mb-20 text-center pt-10">
		<h1
			class="text-5xl md:text-7xl font-bold mb-6 text-primary pb-2 animate-fade-in font-heading"
		>
			Explorando la Web3
		</h1>
		<p
			class="text-xl text-text-muted max-w-2xl mx-auto leading-relaxed animate-slide-up"
			style="animation-delay: 0.1s;"
		>
			Descubre artículos sobre blockchain, contratos inteligentes y el
			futuro de internet.
		</p>
	</section>

	<section>
		<div class="flex items-center justify-between mb-10">
			<h2
				class="text-2xl font-bold text-text-main flex items-center gap-3 font-heading"
			>
				<span class="w-2 h-8 bg-primary rounded-full"></span>
				Artículos Recientes
			</h2>
			<div class="h-px bg-white/10 flex-grow ml-6 hidden md:block"></div>
		</div>
		<div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
			{
				posts.map((post) => (
					<PostCard
						title={post.data.title}
						description={post.data.description}
						pubDate={post.data.pubDate}
						url={`/posts/${post.slug}`}
						tags={post.data.tags}
					/>
				))
			}
		</div>
	</section>
</Layout>
